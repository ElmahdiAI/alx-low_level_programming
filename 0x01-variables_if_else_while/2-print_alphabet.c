#include <stdio.h>
/**
 * main - Entry point
 * Description: 'another C code in ALX program'
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 97; i < 132; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
