#include "main.h"

/*******************************************************
 *********************MEHDI**AI*************************
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 *******************************************************
 *******************************************************/
int _strcmp(char *s1, char *s2)
{
	int i = 0, sum = 0;

	while (*s1 || *s2)
	{
		sum += (s1[i] + '0') - (s2[i] + '0');
		i++;
	}
	return sum;
}
